# 简介
图标库

## 如何引用
引用需要遵循此按需引用的方式
```jsx
import { Tips } from '@nutui/icons-react-taro'

<Tips size={12}/>
```

## 何时使用
需要使用图标展示、图标按钮、图标提示等视觉标识场景时使用

## 注意事项
- 只能使用「可用图标列表」中的图标名，不要自行组合或推测图标名（如 `StarFilled` 并不存在）
- 所有图标均为组件化使用，必须以标签形式引入
- 必须配置 size 属性
